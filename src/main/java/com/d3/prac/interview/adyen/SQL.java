package com.d3.prac.interview.adyen;

public class SQL {

//    SELECT tasks.id AS task_id,
//    tasks.name AS task_name,
//            case
//    when avg(reports.score) > 0
//    and avg(reports.score) <= 20 then 'Hard'
//    when avg(reports.score) > 20
//    and avg(reports.score) <= 60 then 'Medium'
//    when avg(reports.score) > 60 then 'Easy'
//    end as difficulty
//    FROM tasks
//    JOIN reports
//    ON   reports.task_id = tasks.id
//    GROUP BY tasks.id, tasks.name, reports.task_id
//    ORDER BY tasks.id

}
